avg=0
temp_total=0
number_of_args=$#

if [ $# -lt 2 ] ; then
   echo "Opps! I need atleast 2 command line args"
	    fi
for i in $*
do

      temp_total=`expr $temp_total + $i`

      done

      avg=`expr $temp_total / $number_of_args`
      echo "Average of all number is $avg"

